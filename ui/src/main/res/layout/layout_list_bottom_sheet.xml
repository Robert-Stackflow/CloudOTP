<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/layout_list_bottom_sheet_recyclerview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/layout_list_bottom_sheet_cancel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/shape_rect"
        android:backgroundTint="@color/color_selector_card"
        android:clickable="true"
        android:focusable="true"
        android:paddingTop="15dp"
        android:paddingBottom="15dp"
        android:text="@string/cancel"
        android:textAlignment="center"
        android:textColor="@color/text_color_red"
        android:textSize="@dimen/sp16"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/layout_list_bottom_sheet_recyclerview" />
</androidx.constraintlayout.widget.ConstraintLayout>