import 'package:flutter/cupertino.dart';

class ChewieDimens {
  static const double dimen2 = 2;
  static const double dimen4 = 4;
  static const double dimen6 = 6;
  static const double dimen8 = 8;
  static const double dimen10 = 10;
  static const double dimen12 = 12;
  static const double dimen16 = 16;
  static const double dimen20 = 20;
  static const double dimen50 = 50;

  static const Radius radius4 = Radius.circular(dimen4);
  static const Radius radius8 = Radius.circular(dimen8);
  static const Radius radius10 = Radius.circular(dimen10);
  static const Radius radius12 = Radius.circular(dimen12);
  static const Radius radius16 = Radius.circular(dimen16);
  static const Radius radius20 = Radius.circular(dimen20);
  static const Radius radius50 = Radius.circular(dimen50);

  static const BorderRadius borderRadius2 =
      BorderRadius.all(Radius.circular(dimen2));
  static const BorderRadius borderRadius4 =
      BorderRadius.all(Radius.circular(dimen4));
  static const BorderRadius borderRadius6 =
      BorderRadius.all(Radius.circular(dimen6));
  static const BorderRadius borderRadius8 =
      BorderRadius.all(Radius.circular(dimen8));
  static const BorderRadius borderRadius10 =
      BorderRadius.all(Radius.circular(dimen10));
  static const BorderRadius borderRadius12 =
      BorderRadius.all(Radius.circular(dimen12));
  static const BorderRadius borderRadius16 =
      BorderRadius.all(Radius.circular(dimen16));
  static const BorderRadius borderRadius20 =
      BorderRadius.all(Radius.circular(dimen20));
  static const BorderRadius borderRadius50 =
      BorderRadius.all(Radius.circular(dimen50));
}
